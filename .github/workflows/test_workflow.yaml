name: CI-Test-Workflow
on:
  push:
    branches:
      - main
    secrets:
      GHE_PAT_TOKEN:
        required: true

jobs:
  test:
    uses: ./.github/workflows/reuse_tester.yaml
    with:
      dotnet-version: '10.0'
    secrets:
      GHE_PAT_TOKEN: ${{ secrets.GHE_PAT_TOKEN }}